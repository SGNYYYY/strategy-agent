[pytest]
testpaths = tests
addopts = -q
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    network: tests that require network or external APIs
    db: tests requiring database access
